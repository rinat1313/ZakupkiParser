# Agent context

Проектные правила: `.cursor/rules/` (включая доменные ограничения парсера)
Субагенты: `.cursor/agents/` (`lead`, `developer`, `analyst`, `tester`, `llm-specialist`)
Skill поступления задачи: `/postuplenie-zadachi`
Legacy-доменные заметки также в `cursor/rules/analiz.txt`.

При новой задаче сначала применяй skill `postuplenie-zadachi`.
Код только в `DataCode`.
